import torch
import numpy as np

from lib.utils import load_obj, natural_key

info_benchmark = load_obj(f'configs/indoor/3DLoMatch.pkl')
# src_pcd = torch.load(src_path).astype(np.float32)
# tgt_pcd = torch.load(tgt_path).astype(np.float32) 






